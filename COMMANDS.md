# STBD 命令词汇表

> ✅ **已按本表实现**（`src/commands.rs`）—— 命令的解析与执行都在那儿，`update.rs` 只管按键。
> 连词 `&&` 与引号都已落地，见「切分」与「`&&` 的实现」两节。
> 文末另有一张「旧 → 新」对照表，可以当变更记录看。
>
> ⚠️ **`!` 外部命令不属于本表** —— 那是 shell 的语言，我们一个词都不分。
> 但它到底怎么跟本表划清界限，见「外部命令：**不是**本表的一部分」。

## 为什么先定词汇表

命令好不好用，**九成取决于词汇表，一成取决于解析器**。反过来说：

- 词汇表定错了，解析器写得再漂亮，敲命令还是别扭；
- 词汇表定对了，解析器就是个查表机。

所以先花时间把「说什么」定死，再写「怎么解析」。

## 语法总则

```text
:命令名  [位置参数...]  [选项...]  [更多位置参数...]
```

1. **命令名**必须第一个词。它有一张**别名表**（`delete` / `del` / `d` 是同一件事）。
2. **位置参数**保持书写顺序 —— 顺序就是含义（`delete 1 3` 的 1 是起点、3 是终点）。
3. **选项**用 `-x`（短）/ `--xxx`（长），可以出现在**命令名之后的任意位置**：
   `copy --force 1 3` 和 `copy 1 3 --force` 是同一个意思。
   ⚠️ **不能写在命令名之前** —— 第一个词永远是命令名（见下面「明确不做的」）。
4. **不认 `!` 后缀。** 所有开关一律走选项：`:open --force a.rs`、`:back -f`。
   （代价是 `:q!` 这种 vim 肌肉记忆会失效，见文末「`:q!` 消失之后」。）
   ⚠️ 现在 `!` 按键有了新工作（进外部命令模式），所以 `:q!` **更不会**回来了 ——
   `!` 只做「按键」这一件事，不做前缀。见「外部命令：**不是**本表的一部分」。
5. **单独一个 `--`** 表示「后面全是位置参数」，用来打开以 `-` 开头的文件。
6. **`&&`** 连接多个命令：**前一条成功才做后一条**（短路）。
7. **不挑大小写。** 命令名、别名、选项名、关键字参数（`all` / `number` / `path`…）
   写成大写也一样认：`:DELETE 1 2`、`:Q`、`:open a.rs --FORCE`、`:set NUMBER`。
   PowerShell 就这么干，不用为了敲条命令去管 CapsLock 押没押上。
   ⚠️ **边界在「这个词是谁定的」**：我们定的词随便大小写；**你写的字**
   （路径、行号、要复制的文本）一个字节都不动 —— `:open README.MD` 打开的就是
   那个大写名字的文件（Linux 上它和 `readme.md` 是两份东西）。
   实现上「归一」只发生**三次**，各自在那个词「归我们管」的边界上：命令名 / 别名在
   **查表**时、选项名在**解析**时、关键字参数在**执行前**（表在 `src/commands.rs` 的
   `KEYWORDS`，按命令名声明）。归一之后所有比较都是普通的 `==` ——
   大小写这件事不会散到每一个分支上（以前就是每个分支各自去比一遍）。
   前提是别名表里**没有靠大小写区分**的名字（`d` 和 `D` 是同一件事），
   这一点有一条测试钉着，免得以后加别名时悄悄撞上。

### 明确不做的（保持简单）

- **不支持 `--key value`**（值必须用 `--key=value`）。否则解析器得预知每个选项吃几个值，
  还要判断「下一个词是值还是参数」。
- **不支持 `-` 当命令名缩写**。`-d` 到底是「命令」还是「开关」，语法上分不出来。
  命令名变短靠**别名表**，选项靠 `-`/`--`，两个空间彻底分开。
- **不支持选项写在命令名之前**：`-f back` 不行，要写 `back -f`。
  第一个词永远是命令名 —— 这条规矩比「先扫一遍找出第一个非选项的词」简单得多，
  也跟 shell 一致：`-la ls` 在 bash 里就是「一条叫 `-la` 的命令」。写错了会有定向提示。

## 外部命令：**不是**本表的一部分

`!` 是**按键**，不是命令前缀 —— 这一条要先说清楚，否则容易把它想成「另一种 `:`」。

```text
!任意 shell 命令行
```

按 `!` 进的是**外部命令模式**，跟命令模式平行的另一个模式（提示符就是 `!`）。
回车之后**把终端整个让出去**，命令由系统的 shell（`cmd /C` / `sh -c`）执行。

对照一下两边对同一行的处理：

|          | `:` 命令模式                       | `!` 外部命令模式               |
| -------- | ---------------------------------- | ------------------------------ |
| 这行归谁 | 编辑器的命令语言                   | shell 的语言                   |
| 怎么切词 | 我们 `lex`：认 `&&`、认引号        | **一个词都不分**，整行原样传   |
| 引号     | 我们的规矩（两种等价、无转义）     | shell 的规矩（我们根本不看）   |
| `&&`     | 我们的连词（短路，见下文）         | shell 的 `&&`（`cmd` 的 `&&`） |
| 查表     | 查（认不出来就报 Unknown command） | 不查（认不出来是 shell 的事）  |
| 输出     | 一行状态栏                         | 命令自己画，滚动在终端里       |

所以：

```text
:copy && delete        ← 我们的 `&&`：复制成功才删（= 移动）
!git commit -m "a b" && echo done   ← shell 的 `&&`：`cmd` 自己判断
```

**两套语言永远不会撞车**，因为**归谁在按键那一刻就定了**。
这正是当初不要 `:!cmd` 的理由：那样同一个 `:` 入口里要判断「这行归谁」，
就得在一个解析器里做两套语法 —— 而按 `!` 的话，这个问题根本不存在。

> ⚠️ **我们不做捕获**：不分词、不改写、不解析输出、不判断成功失败。
> 好处是编码（GBK）、颜色、交互式程序（`python`、`vim`、`ssh`）、超大输出
> 全都自动正确 —— 终端真的交给它了，我们一个字节都不碰。
> 代价是**输出留不住**（跟随终端滚动），而且**所有后果自己承担**。
> 「把输出抓进只读视图」是另一条路（Vim 的 `:r !`），还没做。

回车之后会看到的：

```text
!git status                    ← 先把刚才那一行重现出来
On branch main                 ← 以下全是命令自己的输出
nothing to commit, working tree clean
[stbd] press ENTER to return   ← 我们印的，敲一下回车就回去
```

**为什么要先重现那一行**：离开备用屏之后，终端是「上一个 shell 提示符」那个状态，
直接蹦出一堆输出很突兀 —— 用户一瞬间不知道刚才跑了什么、是不是自己敲的那条。
先把命令行打出来就有来龙去脉了（Vim 的 `:!ls` 也这样）。
格式跟底部输入行**逐字一致**（同一个 `!`、同样不加空格），所以画面是连续的：
屏幕上接着写出来的，就是你刚才在底栏看到的那一行。

**命令自己的报错不用我们操心**：stdin / stdout / stderr 全部继承给子进程，
所以 `cmd` 的「不是内部或外部命令」、`git` 的 `fatal:`、编译器的诊断
都**原样打在终端上** —— 我们不拦、不改、不翻译。我们只负责自己那两行：
开跑前重现命令行，跑完说一句怎么回去（外加上「连 shell 都没起来」和「退出码非 0」两种我们自己报的）。

中途 Ctrl+C 打断也一样 —— 等的是那一次回车。

## 位置参数

### 顺序：选项无所谓，位置参数**必须**保留（而实现上不用操心）

- **选项带名字**，名字就是它的身份 → 所以顺序无意义，这正是它能随便放的原因。
- **位置参数没名字**，顺序就是它**唯一**的身份：`delete 1 3` 里 1 是起点、3 是终点，
  反了就变成另一个意思 → 所以顺序必须固定。

但**不需要为这件事写任何额外逻辑**，因为「保序」在分类那一步是天然发生的：

```text
copy --force 1 5:7 3   →  args = ["1", "5:7", "3"]   flags = ["force"]
copy 1 5:7 3 --force   →  args = ["1", "5:7", "3"]   flags = ["force"]
```

非选项的词**按遇到的顺序 push 进 `args`** —— 过滤本身就是保序操作，相对顺序自动保住。

所以「固定参数的相对位置」固定的只是**规则**（`delete` 的第二个参数是终点），
不是实现 —— 实现那边一行都不用写。

### 「位置」是一个统一概念：`行` 或 `行:列`

位置参数真正容易变乱的地方，不是顺序，而是**同一个位置放不同形状的东西**。
现在的命令表里就有这个情况：

```text
copy 1 3         # 行 1 到行 3          ← 两个「行」
copy 2:3 5:7     # 从 (2,3) 到 (5,7)    ← 两个「坐标」
```

同样两个位置参数，却有**两种形状**。这叫按形状重载，能用，但有代价：文档要写两套语法；
错误提示也要仔细（`copy 1:2 3` 得说“第一个像坐标、第二个像行号”，而不是“个数不对”）。

**更干净的做法：只存在一种东西叫「位置」，写作 `行` 或 `行:列`（都 1 基）。**

| 写法  | 含义                                              |
| ----- | ------------------------------------------------- |
| `3`   | 第 3 行（当**起点**时指行首，当**终点**时指行尾） |
| `2:5` | 第 2 行第 5 列                                    |

于是 `copy 1 3` 就是 `copy 1:1 3:end` 的简写 —— **一套语法，两种详略**，而不是两套独立语法。
（这跟现在的行为完全一致：现在就是 `(行, 0)` 到 `(行, usize::MAX)`。）

**每个命令声明它接受几个位置、什么形状**：

| 命令     | 接受                              | 例子                                 |
| -------- | --------------------------------- | ------------------------------------ |
| `delete` | 1 或 2 个**行**（不接受 `行:列`） | `delete 3`、`delete 1 3`             |
| `copy`   | 1 或 2 个**位置**                 | `copy 3`、`copy 1 3`、`copy 2:3 5:7` |
| `swap`   | 正好 2 个**行**                   | `swap 1 3`                           |
| `set`    | 1 个名字 + 1 个值                 | `set tabwidth 4`                     |
| `open`   | 1 个路径                          | `open src/main.rs`                   |

`all` 是**保留字**（不算位置参数）：`delete all`、`copy all`。

## 命令表

### 退出与导航

| 命令     | 别名                      | 参数  | 选项         | 说明                                     |
| -------- | ------------------------- | ----- | ------------ | ---------------------------------------- |
| `quit`   | `q` `exit` `qa` `quitall` | —     | `-f/--force` | 退出整个程序                             |
| `back`   | `prev` `bprev`            | —     | `-f/--force` | 回到上一个打开的文档                     |
| `next`   | `bnext` `n`               | —     | `-f/--force` | 去下一个打开的文档                       |
| `ls`     | `files`                   | —     | —            | 列出打开过的文档（`*` 标当前）           |
| `forget` | —                         | `<n>` | —            | 把第 n 个文档从列表里去掉（序号见 `ls`） |

`--force` 对 `quit` 是**空操作**（退出本来就不拦脏数据），所以 `:q --force` 就等于 `:q`
—— 不需要为它写任何特例。这是把 `:q` 定义成「退出」换来的好处。

### 长输出：铺一屏还是写一行

有几条命令的输出**装不下一行**。它们和「一行状态栏」是两种东西：

|                | 干什么       | 例子                                         |
| -------------- | ------------ | -------------------------------------------- |
| **一行状态栏** | 一句话的回执 | `:w` 说 Saved、`:check` 说跑完了             |
| **一屏清单**   | 一份**内容** | `:ls` 有几个文档、`:errors` 这个文件哪些毛病 |

这个标准**没法自动定**，所以是**明说**的：现在只有 `:ls` 和 `:errors` 走清单，
其余全走状态栏。

> ⚠️ `:ls` 以前是拼成**一行**塞进状态栏的。那一行会**从右边被直接切掉**，
> 而且切得没有任何痕迹 —— 屏幕上剩一个看起来挺完整的开头，你会以为自己
> 只打开了三个文件。Windows 路径本来就长，三四个就能撑爆。

### 清单视图

清单是一个**只读的虚拟视图**（不是磁盘上的文件），所以能滚动、光标能走。

> ⚠️ 它会让出一个特殊的坑：**`q` 退回去时会把原来那份文档「原样」放回来，
> 而不是重新打开一个文件**。

清单除了铺到屏幕上，还会**写进输出文件夹**（长输出不该只活在内存里 ——
那样你想拿它去搜、去比对的时候就没辙了）：

```text
:ls     →  <输出文件夹>/file_list.txt
:errors →  <输出文件夹>/error_log.txt
:lsp    →  <输出文件夹>/lsp_status.txt
```

输出文件夹默认在**可执行文件旁边**（与配置文件同一个思路：便携，不往系统盘塞东西）：

```text
<stbd.exe 所在目录>/stbd-out/file_list.txt
<stbd.exe 所在目录>/stbd-out/error_log.txt
<stbd.exe 所在目录>/stbd-out/lsp_status.txt
```

文件名一律小写 ASCII —— 中文名在 shell、脚本和一些工具链里得引号包着，
而我们自己的终端输出本来就是 GBK 花屏的。只有 `stbd-settings.toml` 是
手工编辑的（值里可以写中文），这几个是程序自己写、也可能被别的脚本读的。

拿不到可执行文件目录时退回 `%APPDATA%\stbd\stbd-out\`（其它系统是
`~/.config/stbd/stbd-out/`）。用 `STBD_OUTBOX` 可以整个换掉这个位置。

关于这几个文件的**三条规矩**：

1. **它们始终存在** —— 空着也留着。所以命令不用处理「文件不在」
2. **内容只在那条命令跑的时候改** —— `:ls` 写完就不管了：诊断变了、
   文档列表变了，文件里那行字也不会自己变。想知道现在什么样，再敲一次
3. **只在进入时清一次** —— 上一次那张列表看起来跟这一次的一模一样，
   你会拿着上次的当这次的用。清一次就永远是这一次的

> ⚠️ 规矩 3 原来是「**进入和退出各清一次**」，已经去掉了「退出那次」。
> 它和第 2 条其实矛盾（退出不是「那条命令跑的时候」），而代价是实测出来的：
> `:errors` 之后一 `:q`，`error_log.txt` 就空了。进入那次已经够用 ——
> 不管上次是正常退出还是崩掉，你打开时看到的永远是干净的。
>
> 所以：**内容留到你下次打开编辑器**。想在退出后拿这份清单，就在编辑器
> 还开着的时候读它（另一个窗口 `type` 一下就行）。

> 清单内容铺上去之后也**不刷新**，跟文件一致：诊断变了、文档列表变了，
> 它自己不动。这样「屏幕上这份东西是什么时候的」永远是确定的。

其余规矩：

- `q` / `:back` 退回去：原来那份文档**原样**放回来，**不是**重新读盘
- 里面**不能** `:w` —— 那是我们生成的文本，不是你的代码
- 清单**不进**文档列表（`:ls` 不会把自己列出来，`q` 也不会在清单之间打转）

> ⚠️ 「原样放回来」是重点。重新读盘会**丢掉没保存的改动**，而且 `:back`
> 那条路上还拦着「没保存不许走」—— 两者加起来就是「看一眼错误，回不去了」。
> 所以进清单时会把整份文档快照下来（rope + `Arc`，O(1)），退出时原样放回去。

### 文档与配置

| 命令            | 别名              | 参数       | 选项         | 说明                                     |
| --------------- | ----------------- | ---------- | ------------ | ---------------------------------------- |
| `open`          | `e` `edit` `stbd` | `<path>`   | `-f/--force` | 打开文件或目录（目录显示子项列表）       |
| `write`         | `w`               | `[<path>]` | —            | 保存当前文档；给了路径就是**另存为**     |
| `wq`            | —                 | —          | —            | 保存并退出                               |
| `settings`      | —                 | —          | `-f/--force` | 打开配置文件来改（不存在就按模板生成）   |
| `reload`        | —                 | —          | —            | 重读配置文件（配合 `settings` 改完保存） |
| `config`        | —                 | —          | —            | 显示当前生效的设置                       |
| `config path`   | —                 | —          | —            | 显示配置文件在哪                         |
| `config edit`   | —                 | —          | `-f/--force` | 同 `settings`                            |
| `config reload` | —                 | —          | —            | 同 `reload`                              |

**`config` 这个族保留子命令**是刻意的：它确实是「同一个东西的四种问法」，
拆成四个平铺名字（`configpath`？）反而更难记。**族只在真的成组时才用。**

### 编辑

| 命令     | 别名       | 参数                                             | 选项 | 说明                 |
| -------- | ---------- | ------------------------------------------------ | ---- | -------------------- |
| `delete` | `del` `d`  | `<行>` / `<起> <止>` / `all`                     | —    | 删行（1 基，含两端） |
| `copy`   | `yank` `y` | `<行>` / `<起> <止>` / `<r>:<c> <r>:<c>` / `all` | —    | 复制到系统剪贴板     |
| `swap`   | `sw`       | `<行> <行>`                                      | —    | 交换两行             |
| `insert` | `i`        | —                                                | —    | 进编辑模式           |
| `undo`   | `u`        | —                                                | —    | 撤销一步             |
| `redo`   | —          | —                                                | —    | 重做一步             |

**`d` / `y` 和 vim 一致**（`:d` 删、`:y` 复制），所以这两个单字母别名值得留。

### 设置

| 命令                          | 别名 | 参数      | 选项 | 说明            |
| ----------------------------- | ---- | --------- | ---- | --------------- |
| `set number` / `set nonumber` | —    | —         | —    | 显示 / 隐藏行号 |
| `set tabwidth <n>`            | —    | `1..=16`  | —    | Tab 空格数      |
| `set scrolloff <n>`           | —    | `0..=100` | —    | 上下边距        |
| `set sidescrolloff <n>`       | —    | `0..=100` | —    | 左右边距        |
| `set lspmaxservers <n>`       | —    | `0..=8`   | —    | 语言服务器个数  |

`set` 保持 vim 的样子。想让打字更短，以后可以再认 `set tw=4` 这种 `=` 形式 + 短名
（`tw` / `so` / `siso`），那不是重构的一部分，随时能加。

### 语言服务器

编辑器**一行分析代码的逻辑都没有** —— 语法、类型、借检查全交给外部的语言服务器，
它只负责把服务器推回来的诊断画在行号栏上。

内置认三样：

| 文件                                    | 起什么          | 怎么给项目根                |
| --------------------------------------- | --------------- | --------------------------- |
| `.rs`                                   | `rust-analyzer` | 往上找 `Cargo.toml`         |
| `.c` `.h`                               | `clangd`        | 用文件自己所在的目录        |
| `.cpp` `.cc` `.cxx` `.hpp` `.hxx` `.hh` | `clangd`        | 同上（和 C 共用同一个进程） |

这两个都得**你自己装**（编辑器不带）：`rust-analyzer` 随 rustup 就有
（`rustup component add rust-analyzer`），`clangd` 随 LLVM 发行版就有。
装了但不在 `PATH` 上，就在配置里写**绝对路径**（见下）。

**加一门语言不用改代码，改配置就行**：

```toml
[lsp.zig]
command = "zls"
extensions = ["zig"]
```

`[lsp.<名字>]` 一节一门语言，名字就是它的 `languageId`。能写的字段：

| 字段          | 必填 | 默认     | 说明                                                      |
| ------------- | ---- | -------- | --------------------------------------------------------- |
| `command`     | ✅    | —        | 起什么命令。**留空 = 这门语言不要了**（用来关掉内置的）   |
| `extensions`  | ✅    | —        | 管哪些扩展名。**小写、不带点**（写 `"rs"`，不是 `".rs"`） |
| `args`        | —    | 空       | 命令行参数，一个词一个                                    |
| `root_marker` | —    | 不要求根 | 往上找这个文件当项目根；不写就用文件自己所在的目录        |
| `language_id` | —    | 节名     | `didOpen` 里报的语言名                                    |

它是**合并**进内置表，不是替换：

- 同名的节（`[lsp.rust]`）**盖住**内置那条 —— 想改默认值就写它；
- 新名字的节（`[lsp.zig]`）就是**加一条**，内置的照样在；
- 所以你可以只写一节，不必把三节全抄一遍。

⚠️ 两条容易踩的：

- **`extensions` 忘了写会直接报错并指出行号。** 故意不给默认值 ——
  给个空默认只会让它安静地匹配不上任何文件，那种错最难查。
- **`language_id` 不是装饰**：`clangd` 靠它决定把 `.h` 当 C 还是当 C++ 解析。
  所以 C 和 C++ 分成两节而不是靠扩展名猜；两节的 `command` 一样，
  所以只起**一个**进程。

`set` 里那条 `lspmaxservers` 管的是**最多允许几个**（默认 3）：

| 值  | 会发生什么                                                   |
| --- | ------------------------------------------------------------ |
| `0` | 完全不用语言服务器（要最省就写它）                           |
| `1` | 只留当前这个项目的；跳项目 = 每次都要重新加载                |
| `3` | 默认：够放「库 + 用它的程序」，也够放一个混着 C 的 Rust 项目 |

⚠️ **它和别的设置不一样**：调小之后多出来的服务器会**当场被收掉**，
不等重启 —— 你敲完就能看到内存降下去。其余设置都是「只影响以后」。

会话是**用到了才起**的，不是预先开好的。所以你只在一个项目里干活时，
写 `1` 和写 `8` 完全一样（都只有一个进程）。

⚠️ **它数的是「进程」，不是「项目」。** 钥匙是「命令 + 项目根」，所以一个
Rust + C 混着的项目**自己就要两个**（`rust-analyzer` + `clangd`）。
写 `2` 的时候这种项目已经把名额吃满，再跳一次项目就要重新加载 —— 那就把
`lspmaxservers` 调大一点。

**关于内存**：一个加载完的 `rust-analyzer` 实测约 1.2 GB（clangd 小得多）。
数字写在那儿，该填几由你的机器决定。

**关于 C / C++ 的假错误**：没有 `compile_commands.json` 时 clangd 也能跑，
但只有语法级的判断 —— 「找不到 `stdio.h`」这类会一堆。生成它：
CMake 加 `-DCMAKE_EXPORT_COMPILE_COMMANDS=ON`，其它构建系统用 `bear -- make`。
（真有了它，clangd 自己会往上找，我们不用管。）

**`set lspmaxservers` 到底填几**，敲一次 `:lsp` 就知道了 —— 它会告诉你
现在跑着几个、上限几个。

### 诊断

| 命令     | 别名 | 参数 | 选项 | 说明                                             |
| -------- | ---- | ---- | ---- | ------------------------------------------------ |
| `errors` | —    | —    | —    | 把当前文件现在的毛病列成一份只读清单             |
| `lsp`    | —    | —    | —    | 语言服务器的现状：配了哪些、找不找得到、跑着几个 |

诊断平时就长在**行号上**：出错的那一行行号变红、警告变黄，其余不动。

为什么只有行号：**我们不知道问题在哪一列**（理由见 `src/diagnostic.rs` 的文件头）。
既然只知道是「哪一行」，能给的最精确的标记就是那一行 —— 画一个假的列位置
比不画更糟，它会引你去一个无关的位置。

光标停在哪一行，最底下的状态栏就显示那一行的诊断，**原文一字不改** ——
那句话是拿去搜的抓手，`cargo` 报的是同一句。

`:errors` 换上一份清单（按行号排好，一条一行）。

- 里面**不能** `:w` —— 那份清单是我们生成的，不是你的代码
- `q`（或 `:back`）退回去：原来那份文档**原样**放回来，**不是**重新读盘

> ⚠️ 最后那句是重点。重新读盘会**丢掉没保存的改动**，而且 `:back` 那条路上
> 还拦着「没保存不许走」—— 两者加起来就是「看一眼错误，回不去了」。
> 所以进清单时会把整份文档快照下来（rope + `Arc`，O(1)），退出时原样放回去。

### `:lsp` —— 「我都没下载，它怎么跑的」

语言服务器是**外部程序**：编辑器不带、也不会替你装。于是最常出现的两个问题
正好相反：「我都没下载，它怎么跑起来的」和「我明明装了，怎么不动」。

`:lsp` 就是这两个问题的答案：

```text
3 server(s) configured, 1 running, limit 3

c  [c h]  ->  clangd
    ok  D:\Dev_Evens\_LLVM\...\bin\clangd.exe
cpp  [cpp cc cxx hpp hxx hh]  ->  clangd
    ok  D:\Dev_Evens\_LLVM\...\bin\clangd.exe
rust  [rs]  ->  rust-analyzer
    ok  C:\Users\...\rustup\...\rust-analyzer.exe

running now (1)
    clangd  @  file:///D:/tmp/probe
```

三种读法：

| 你看到的      | 意思                                                                   |
| ------------- | ---------------------------------------------------------------------- |
| `ok  <路径>`  | 这个命令找到了，去找的就是这个文件（**路径**能告诉你是不是你想的那个） |
| `NOT on PATH` | 这个命令**没找到**。编辑器照常用，但那种文件不会有诊断                 |
| `off:`        | 这一节的 `command` 是空的 —— 你自己把它关掉了                          |

> ⚠️ **`NOT on PATH` 不等于你没装。** 我们**只认 `PATH` 上的命令**。
> VS Code 扩展**打包在里面**的那些服务器（pyright、jdtls……）不在 `PATH` 上，
> 我们也看不见 ——「装了扩展」和「我们找得到」是两件不相干的事。
> 想用就把那个程序的**绝对路径**写进 `command`。

`running now` 是空的也**不代表坏了**：服务器是**用到了才起**的，而且只在真文件上起 ——
停在目录列表里、或者这门语言的文件还没打开过，本来就该是空的。

> 它和 `:errors` / `:ls` 一样：铺成一份只读清单，`q` 退回去时原来那份文档
> **原样放回**；同时写进 `<输出文件夹>/lsp_status.txt`；不需要 `--force`。

### 后台任务

| 命令     | 别名  | 参数 | 选项 | 说明                                               |
| -------- | ----- | ---- | ---- | -------------------------------------------------- |
| `check`  | —     | —    | —    | 在后台跑 `cargo check`，跑完把 cargo 的总结报出来  |
| `format` | `fmt` | —    | —    | 在后台跑格式化工具，结果换回**缓冲区**（不碰磁盘） |

`check` **不等结果**：按下回车那一刻状态栏就显示 `Checking…`，跑完再把结果报出来。
整个过程中按键照常响应、屏幕照常刷新。

做成「后台」而不是「阻塞等它跑完」是有原因的：`cargo check` 要几百毫秒到几秒，
阻塞的话那几秒里按键不响应，等于卡死。

> ⚠️ 它**只报 cargo 自己写的那句总结**，不报「几条错误」。
> 想过从人能看的输出里数条数 —— 但 `error: could not compile …` 这句总结也以 `error`
> 开头，`warning: … generated 1 warning` 同理，数出来会**永远虚高**。
> 而一个虚高的错误数比没有错误数更糟：它会让人不再信这个功能。
>
> 真要「哪一行、什么错」得解析 `--message-format=json`。那件事会和 LSP 的诊断
> **共用同一套结构**（行号 + 级别 + 原文），所以留到那一步一起做 —— 只做一次。

#### `:fmt` —— 排版是**别人的活**，我们只当话务员

`:fmt` 是**项目里第一个「提供者」**。它自己**一行排版逻辑都没有**：按后缀查一张表
挑一个外部工具，把文本喂进去，把结果拿出来。

三条规矩：

1. **排的是缓冲区，不是磁盘上那个文件。** 缓冲区里可能有**没保存的改动** ——
   让工具去读盘，它排的是旧内容，我们再拿结果一换，你刚敲的东西就一声不响地没了。
   所以走的是「stdin 进、stdout 出」，它**一个字都不许写盘**。
2. **它占一个撤销步。** 排版是**可以有主观看法的**（长表达式怎么折、换行放哪），
   不满意时一个 `u` 就退回去。这也是它和 `:open` 的分界：`open` 是「换成另一份文档」
   （所以清掉撤销栈），而排个版清掉撤销栈是不可接受的。
3. **一个字都没变时不记撤销步**，状态栏说 `Format: already clean` ——
   否则连敲两次 `:fmt` 之后要按两次 `u` 才能退掉一次真正的编辑。

现在有两个提供者：

| 工具           | 认的后缀                                          | 状态栏上叫     |
| -------------- | ------------------------------------------------- | -------------- |
| `rustfmt`      | `.rs`                                             | `rustfmt`      |
| `clang-format` | `.c` `.h` `.cpp` `.cc` `.cxx` `.hpp` `.hxx` `.hh` | `clang-format` |

clang-format 那批后缀是跟配置里 `[lsp.c]` / `[lsp.cpp]` **对齐**的：
语言服务器认的文件，格式化器也该认 —— 两处不一致只会让人困惑
（「为什么这里有诊断却没有格式化」）。

别的后缀会说 `No formatter for this file type`，
**不会**先闪一下「Formatting…」再改口。

> ⚠️ **那道门是刻意留的**：命令层里**不许**出现 `if 语言 == "rust" { 跑 cargo fmt }`，
> 而是去 `src/formatter.rs` 的表里查。将来能不能接第三个（gofmt、prettier……），
> 只由这一处决定。（VS Code 的内置扩展走的是跟第三方**完全同一套**机制 ——
> 那才是「可扩展」的实质，不是多建一个文件夹。）

#### 喂了 stdin，就得把被抹掉的上下文还回去

这是接这两个工具时唯一的**通用**教训。走 stdin 就是把「这个文件叫什么、在哪个项目里」
抹掉了，而两个工具都要这点信息 —— 只是要的**不是同一块**：

| 工具           | 要还什么                                                              | 少了它会怎样（都实测过）                                                                                                                     |
| -------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `rustfmt`      | `--edition <版本>`（从往上找 `Cargo.toml` 的 `[package] edition` 来） | 它按 **2015** 解析，而 `if let Some(x) = y && cond`（let-chain）是 2024 的语法 —— **这个仓库自己在用**，于是 `:fmt` 在它自己的源码上报语法错 |
| `clang-format` | `-style=file` + `-assume-filename=<真路径>`                           | 不给这两条 → 它用**内置 LLVM style**，项目里的 `.clang-format` 一个字都不看。实测同一份输入：2 空格 vs 4 空格                                |

> ⚠️ 第二条尤其值得说：**这大概就是「clang-format 好像没格式化」的头号原因。**
> 它拿一套你并不认同的默认值把你的代码排成了另一个样子，而那些本来就接近
> LLVM style 的代码，输出会和输入**一模一样**。
>
> 另一个常见原因是**命令行的用法**：`clang-format x.c` 是把结果**打到 stdout**，
> 文件不动；要改文件得 `clang-format -i x.c`。这跟编辑器里没关系，但值得知道。

> ⚠️ **`-assume-filename` 一个人就顶替了 cwd。** 实测：只给 `-style=file`、
> 而 cwd 在项目外 → 仍然 2 空格；补上 `-assume-filename=<真路径>`（cwd 还在项目外）
> → 4 空格。所以那个参数不是「多给一点信息」，它是**唯一**让配置被发现的东西。

> ⚠️ **换行在这里统一兜，不给工具各配参数。**
>
> 我们的缓冲区**只认 `\n`**（ropey 关掉了 `unicode_lines` / `cr_lines`），
> 所以提供者交回来的东西必须是纯 LF —— 否则一份 CRLF 进了缓冲区，会变成
> 「若干行、每行尾多一个 `\r`」，而那个 `\r` 是个**真字符**（参与列号计算、
> 存盘也会原样写出去）。
>
> 这个归一化只有一处：**`formatter::run` 的出口**。所以**每个**提供者
> （现在两个，将来更多）都不可能把 `\r` 塞进来。
>
> 为什么不给每个工具加参数：**它们在这件事上行为不一样**，而且改过一轮 ——
>
> | 工具 | 行为（都实测过） |
> | ---- | ---------------- |
> | `rustfmt` | `newline_style` 默认 `Auto` 是「跟着输入走」，但输入里**一个换行都没有**时探测不出来，退回 Windows 原生 CRLF |
> | `clang-format` | **真的**跟着输入走：CRLF 进 → CRLF 出 |
>
> 第一版是「`rustfmt` 加 `--config newline_style=Unix`，`clang-format` 不用管」
> —— 而**后半句是错的**：当时只量了 LF 输入就下了结论。
> 靠「给每个工具各配一个参数」就是要**每个都量一次、各配一次**，
> 而且它们的行为还会随版本变。在出口兜住只需要对一次。
>
> ⚠️ **副作用要说清：`:fmt` 会顺手把 CRLF 文件的整份行尾改成 LF。**
> 在这个强制 LF 的仓库里那是**修正**而不是破坏（`.gitattributes` 就是 `eol=lf`）；
> 而我们本来也忠实地表示不了 CRLF 文件。这也意味着：
> 一个不小心写成 CRLF 的新文件，`:fmt` + `:w` 就把它修好了。

> ⚠️ **clang-format 对「编译不过的代码」不会失败** —— 它照样排。
> 实测 `int main(){int x = ;}` 进去，它 exit 0、照样把空格补齐。
> 所以 C/C++ 文件上 `Format: FAILED` 会**很罕见**，那是正常的，不是坏了；
> 反过来也别指望它是语法检查器。

### 选项清单（v1 只做这一个）

| 选项      | 短写 | 含义                     | 哪些命令认                                                          |
| --------- | ---- | ------------------------ | ------------------------------------------------------------------- |
| `--force` | `-f` | 跳过「未保存改动」的拦截 | `quit`（空操作）、`back`、`next`、`open`、`settings`、`config edit` |

> 实现上 `config` **整族**都接受 `--force`（在 `config` / `config path` / `config reload`
> 上是空操作）—— 跟 `quit --force` 同一种处理。只有 `config edit` 真的用得上它。
> 为一个子命令加特例，不如让「声明」和「校验」继续共用一张表。

**选项只在声明的命令上有效** —— 在 `delete` 上写 `--force` 应该报「不认识」，
而不是静默忽略。否则打错一个字母就没反应，很难查。

以后的候选（先记下，不做）：`--readonly`（打开时进只读）、`--at=<r>:<c>`（定位到坐标）。

## 实现要点（供参考）

分**两阶段**，别写成「边走分支边消费参数」：

```text
① 切分+分词  "copy 1 3 && delete 1 3"  →  [词 copy, 词 1, 词 3, &&, 词 delete, 词 1, 词 3]
② 分段      按 `&&` 切开              →  [[copy,1,3], [delete,1,3]]
③ 分类      → 命令名 / 标志集合 / 位置参数（保持相对顺序）
④ 匹配      (名字别名归一化, 位置参数形状) 查表；标志从集合里问
⑤ 执行      命中 → 干活；没命中 → 报该命令的用法
```

① 和 ② **合成了一次扫描**（`lex`）：分词的边界和 `&&` 的边界是**同一套规则**
（都是「不在引号里才算」），拆两次写就是抄两遍状态机。

**③ 和 ④ 必须分开。** 「`--force` 放前面放后面都一样」是 ③ 的性质；
「走不走这个分支」是 ④ 的事。混在一起就会出现「为什么这个顺序不行」这类玄学 bug。

⑤ 拿到的是「命令 + 选项」，用一个小结构承载：

```rust
struct Command<'a> {
    name: &'a str,        // 已归一化到规范名，大小写也算（d / D / Del → delete）
    flags: Vec<&'a str>,  // ["force"]
    args: Vec<&'a str>,   // ["1", "3"]，保持原顺序
}
```

### `&&` 的实现（已落地）

当初估的三条代价，实际是这么兑的：

1. `handle_key_event` / `execute_command` 的返回类型从 `Option<Action>` 变成
   `Vec<Action>`，main 改成 `for action in ... {}` 按序执行。这一步几乎没阻力。
2. **真正要解决的是「成功」和「失败」长得一样。** 解法不是加个 `ok: bool` 字段，
   而是让**失败走返回值**：

   ```rust
   type Executed = Result<Option<Action>, String>;
   ```

   - `Ok(Some(action))` —— 成功，而且要 main 去做点什么
   - `Ok(None)` —— 成功，只是状态变化
   - `Err(提示)` —— 失败

   顺带的好处：**各个 handler 不再自己写状态栏**，只负责返回 `Err`，
   在哪呈现由 `run` 一处决定。「成功走状态栏、失败走返回值」这条分界，
   其实就是「失败是控制流、成功只是汇报」。
3. 引号（当时还没有）已经补上了 —— 所以 `&&` 现在可以出现在路径里，
   只要用引号包住：`open "a&&b.txt"`。引号里的 `&&` 不是分隔符。
   实现见下一节。

判定「成功」的规矩：**「该做的事没做成」就算失败。** 明显的错（`delete 99` 越界）算；
「现状播报」也算 —— `undo` 撤到头了、`back` 已经在第一个，都是没做成。
统一成一条规矩比逐个争论可靠，而且短路是**保守的方向**：只会让链少做点，绝不会多做。

**为什么是 `&&` 而不是 `&`**：`&&` 在 shell / C / Rust / 逻辑学里**只有一个意思** ——
「并且，且左边不成就不做右边」；`&` 在这几个地方分别是「后台跑」「按位与」「非短路」，
含义随语境漂。更关键的一条：**「无条件都执行」本来就不需要语法**，多敲一行命令就是了
（`:` 两次）。真正需要专门语法的，是「**有条件地**执行」。

好消息：`Action::Copy(String)` 的载荷是**当场算好带出去的**，所以
`copy 1 3 && delete 1 3`（复制完再删 = **移动**）能正确工作。
顺带一提，它也是短路的**头号理由**：复制失败还接着删，那几行就真没了。

## 旧 → 新 对照表（重构 checklist）

| 现状                                              | 目标                               | 备注                                     |
| ------------------------------------------------- | ---------------------------------- | ---------------------------------------- |
| `:q` / `:quit`                                    | `:q` = **退出**                    | ⚠️ 语义变更，见文末                       |
| `:exit` / `:qa` / `:quitall`                      | `:quit` 的别名，保留               |                                          |
| `:q!`                                             | **废弃** → 用 `:q`                 | vim 肌肉记忆会踩空，需配友好提示（见下） |
| `:prev` / `:bprev`                                | `:back`（别名保留）                |                                          |
| `:next` / `:bnext`                                | `:next`（加别名 `n`）              |                                          |
| `:ls` / `:files` / `:forget`                      | 不变                               |                                          |
| `:stbd` / `:open` / `:e <path>`                   | `:open`（别名 `e`/`edit`/`stbd`）  |                                          |
| `:stbd!` / `:prev!` / `:next!` / `:settings!`     | `--force` / `-f`（`!` **不再认**） | **arm 爆炸就消在这里**                   |
| `:settings` / `:config edit`                      | 不变                               |                                          |
| `:config` / `:config path`                        | 不变                               |                                          |
| `:config reload`                                  | 保留，另加顶层 `:reload`           |                                          |
| `:swap single line x y`                           | `:swap x y`                        | 删噪音 `single line`                     |
| `:delete line x`                                  | `:delete x`                        | 删噪音 `line`                            |
| `:delete line s l`                                | `:delete s l`                      |                                          |
| `:copy line x` / `:copy line s l`                 | `:copy x` / `:copy s l`            |                                          |
| `:copy 2:3 5:7`                                   | 不变                               | 含 `:` 即坐标                            |
| `:delete all` / `:copy all`                       | 不变                               |                                          |
| `:set ...` / `:w` / `:wq` / `:u` / `:redo` / `:i` | 不变                               |                                          |

## 已拍板（2026-09）

1. **`:q` / `:quit` = 退出程序**；**`:back` = 返回上一级**。`:q` 不再「智能返回」。
2. **不用 `!`**，开关一律 `-` / `--`。
3. **`q` 键保持智能**：有上一级就返回，否则退出（ranger 手感保住）。

### 按键与命令**允许**同名不同义（第 3 条带来的）

|          | 写法                     | 含义                     |
| -------- | ------------------------ | ------------------------ |
| **按键** | `q`                      | 有上一级就返回，否则退出 |
| **命令** | `:q` / `:quit` / `:exit` | 直接退出                 |
| **命令** | `:back`                  | 直接返回上一级           |

看起来矛盾，但这其实是**两套语言**：

- **按键**要的是**手感** —— `q` 一层层退回，一个键走遍全树；
- **命令**要的是**确定性** —— `:q` 永远退出，写进脚本/组合进 `&&` 时不会有歧义。

vim 自己就满是这种不对称：`x` 删一个字符，但 `:x` 是「保存并退出」。
所以这不是将就，是刻意的分工。

### 别名要不要这么激进

`:d` / `:y` / `:n` / `:sw` 这些单字母别名很好敲，但会占用短名字。
别名是最容易改的东西（加一行查表），所以先按上面的定，不合适随时删。

## `:q!` 消失之后 —— 已实现

不认 `!` 之后，vim 用户条件反射敲的 `:q!` 会落到「未知命令」。**只报一句「未知命令」
是不够的** —— 他以为是自己敲错了字，而真正的原因是写法变了。所以名字以 `!` 结尾、
去掉之后真是个已知命令时，给一句定向提示：

```text
Unknown command: q!      —  `!` 已废弃，要强制就用 `:q --force`
Unknown command: delete! —  `!` 已废弃，直接用 `:delete`
```

分两句是因为**不认 `--force` 的命令不能瞎撺掇**：`delete --force` 会被选项校验拒掉，
让用户绕一圈再碰一鼻子灰，比不说还糟。去掉 `!` 之后不是已知命令时（`:nope!`），
就回到普通的 `Unknown command`。

## 引号（已落地）

实现落在 [`lex`]（`src/commands.rs`）—— 它是命令层**唯一**有状态的地方。

```text
:open "my file.rs"      # 空格不再是边界
:open "a&&b.txt"       # `&&` 也不再是边界
:open "it's.txt"       # 引号只在词首才特殊 —— 撇号就只是个普通字符
:open 'it"s.txt'       # 要引号本身，换一种引号包
```

从 POSIX shell 里挑的是**本质**，砍掉的是伺候「展开」的零件：

| POSIX 有                          | 这里                 | 为什么                                                                                                                                                |
| --------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `'...'` / `"..."` 收词            | ✅ 都有，**完全等价** | 两种引号在 shell 里的区别（`"` 里还展开 `$`）在这里不存在 —— 我们没有展开                                                                             |
| `\` 转义                          | ❌ 不做               | 转义是为了区分「展开出来的引号」和「字面上的引号」。没有展开就没那层区分。代价：同时含 `'` 和 `"` 的名字写不出来（Windows 上 `"` 本来就不许进文件名） |
| 相邻片段拼接（`a"b c"d`）         | ❌ 报错               | 拼接是为了把「展开的结果」和字面量接上（`"$dir"/x`）。我们没有展开可拼；而支持它就得让词变成拥有所有权的 `String`。报错是为了别让它静悄悄变成两个词   |
| 引号没关上 → 换个提示符等你接着敲 | ❌ 报错               | 命令模式一行一执行，等不了                                                                                                                            |

**一条容易忘但很关键的规矩**：*引号只改「边界」，不改「身份」*。
`ls "-la"` 和 `ls -la` 是一回事，`open "-x"` 里的词**仍然**算选项。
想让一个以 `-` 开头的词当参数，只能用 `--` —— 这也正是 `--` 存在的理由。

